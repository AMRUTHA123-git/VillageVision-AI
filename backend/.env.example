# VillageVision AI Backend Environment Variables
PORT=5000
FLASK_DEBUG=False
# Set this to your deployed Vercel frontend URL on Render (e.g. https://villagevision-ai.vercel.app)
FRONTEND_URL=https://your-app.vercel.app
# Optional: Path to SQLite DB if using a persistent disk mount on Render (e.g. /var/data/village_vision.db)
DATABASE_PATH=
